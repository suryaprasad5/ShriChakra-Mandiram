window.SCM_CONTENT_CONFIG = {
  // Preferred: one Apps Script web app that returns events, gallerySections, and sevas for action=read.
  APPS_SCRIPT_URL: '',

  // Optional CSV fallbacks if each Google Sheet tab is published separately.
  EVENTS_CSV_URL: '',
  GALLERY_SECTIONS_CSV_URL: '',
  SEVAS_CSV_URL: '',
};
